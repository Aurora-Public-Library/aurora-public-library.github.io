"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.readSessionStorageItem = readSessionStorageItem;
exports.removeSessionStorageItem = removeSessionStorageItem;
exports.writeSessionStorageItem = writeSessionStorageItem;

/**
 * Retrieves data from the sessionStorage, ensuring it's original data type.
 *
 * @param {string} key   The key whose value to retrieve.
 * @returns  The parsed data from the sessionStorage, or null if not found.
 */
function readSessionStorageItem(key) {
  if (typeof window !== 'undefined') {
    var stored = window.sessionStorage.getItem(key);

    if (stored) {
      return JSON.parse(stored);
    }
  }

  return null;
}
/**
 * Saves data to the sessionStorage
 *
 * @param {string} key   The key to save the data to.
 * @param {any} value    The data to save. Supports all JSON data types: https://restfulapi.net/json-data-types/
 */


function writeSessionStorageItem(key, value) {
  if (typeof window !== 'undefined') {
    window.sessionStorage.setItem(key, JSON.stringify(value));
  }
}
/**
 * Removes data from the sessionStorage.
 *
 * @param {string} key   The key to remove
 */


function removeSessionStorageItem(key) {
  if (typeof window !== 'undefined') {
    window.sessionStorage.removeItem(key);
  }
}